Q - tree bottom view GFG 115 .
Ans - using BFS .
class Solution:
    def bottomView(self, root):
        # code here
        if not root:
            return None
        ans = []
        result = {}
        queue = deque()
        queue.append([root,0])
        while root:
            e,line = queue.popleft()
            result[line] = e.data
        
            if e.left:
                 queue.append(e.left , line - 1)
            if e.right:
                queue.append(e.right, line + 1)
        for value in sorted(result.items()):
            ans.append(value[1])
        return ans 
TC-O(n log n) #sorting

SC -O(H)
           
       
